import {
  require_jsx_runtime
} from "./chunk-4E4UAEVW.js";
import {
  require_react
} from "./chunk-6U6HN6A3.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/.pnpm/@ssgoi+core@4.4.0/node_modules/@ssgoi/core/dist/types.js
function u(t2) {
  return "items" in t2;
}
async function h(t2) {
  const e2 = await t2;
  return u(e2) ? {
    prepare: e2.prepare,
    wait: e2.wait,
    onReady: e2.onReady,
    onStart: e2.onStart,
    onEnd: e2.onEnd,
    items: e2.items,
    schedule: e2.schedule
  } : {
    prepare: e2.prepare,
    wait: e2.wait,
    onReady: e2.onReady,
    onStart: e2.onStart,
    onEnd: e2.onEnd,
    items: [
      {
        physics: e2.physics,
        tick: e2.tick,
        css: e2.css
      }
    ],
    schedule: "parallel"
  };
}
function k(t2, e2) {
  const a2 = t2.schedule ?? "parallel", r4 = t2.items.map(
    (i2) => {
      let n4;
      switch (a2) {
        case "parallel":
          n4 = 0;
          break;
        case "sequential":
          n4 = 1;
          break;
        case "stagger":
          n4 = Math.max(0, Math.min(1, i2.offset ?? 0));
          break;
        default:
          n4 = 0;
      }
      const { css: s, offset: m2, ...c3 } = i2, l = s ? typeof s == "function" ? e2 ? { element: e2, style: s } : void 0 : { element: s.element, style: s.style } : void 0;
      return { ...c3, css: l, normalizedOffset: n4 };
    }
  ), { schedule: d5, ...o2 } = t2;
  return {
    ...o2,
    items: r4
  };
}

// node_modules/.pnpm/@ssgoi+core@4.4.0/node_modules/@ssgoi/core/dist/provider-Cu4NSF1Q.js
var I = Object.defineProperty;
var _ = (a2, e2, t2) => e2 in a2 ? I(a2, e2, { enumerable: true, configurable: true, writable: true, value: t2 }) : a2[e2] = t2;
var n = (a2, e2, t2) => _(a2, typeof e2 != "symbol" ? e2 + "" : e2, t2);
var x = 0.05;
var b = class {
  constructor(e2) {
    n(this, "omega");
    n(this, "zeta");
    n(this, "restDelta");
    n(this, "restSpeed");
    const { stiffness: t2, damping: s } = e2, i2 = 1;
    this.omega = Math.sqrt(t2 / i2), this.zeta = s / (2 * Math.sqrt(t2 * i2)), this.restDelta = e2.restDelta ?? 0.01, this.restSpeed = e2.restSpeed ?? 0.01;
  }
  step(e2, t2, s) {
    const i2 = Math.min(s, 0.033), { omega: r4, zeta: p4 } = this, { position: c3, velocity: d5 } = e2, h4 = -2 * i2 * p4 * r4 * d5, o2 = i2 * r4 * r4 * (t2 - c3), l = d5 + h4 + o2;
    return {
      position: c3 + i2 * l,
      velocity: l
    };
  }
  isSettled(e2, t2) {
    const s = Math.abs(t2 - e2.position), i2 = Math.abs(e2.velocity);
    return s < this.restDelta && i2 < this.restSpeed;
  }
};
var H = class {
  constructor(e2) {
    n(this, "leader");
    n(this, "follower");
    n(this, "restDelta");
    n(this, "restSpeed");
    this.restDelta = e2.restDelta ?? 0.01, this.restSpeed = e2.restSpeed ?? 0.01, this.leader = new b({
      stiffness: e2.stiffness,
      damping: e2.damping,
      restDelta: this.restDelta,
      restSpeed: this.restSpeed
    });
    let t2, s, i2, r4;
    typeof e2.follower == "number" ? (t2 = e2.stiffness * e2.follower, s = e2.damping) : e2.follower ? (t2 = e2.follower.stiffness, s = e2.follower.damping, i2 = e2.follower.restDelta, r4 = e2.follower.restSpeed) : (t2 = e2.stiffness, s = e2.damping), this.follower = new b({
      stiffness: t2,
      damping: s,
      restDelta: i2 ?? this.restDelta,
      restSpeed: r4 ?? this.restSpeed
    });
  }
  step(e2, t2, s) {
    const r4 = e2._leader ?? {
      position: e2.position,
      velocity: e2.velocity
    }, p4 = this.leader.step(r4, t2, s), c3 = this.follower.step(
      e2,
      p4.position,
      s
    );
    return {
      position: c3.position,
      velocity: c3.velocity,
      _leader: p4
    };
  }
  isSettled(e2, t2) {
    const s = e2, i2 = Math.abs(t2 - e2.position) < this.restDelta && Math.abs(e2.velocity) < this.restSpeed;
    return s._leader ? Math.abs(t2 - s._leader.position) < this.restDelta && Math.abs(s._leader.velocity) < this.restSpeed && i2 : i2;
  }
};
var L = 0.01;
var M = 500;
var v = 10;
var F = class {
  constructor(e2) {
    n(this, "acceleration");
    n(this, "resistance");
    n(this, "resistanceType");
    n(this, "min");
    n(this, "max");
    n(this, "bounceStiffness");
    n(this, "bounceDamping");
    n(this, "restDelta");
    this.acceleration = e2.acceleration, this.resistance = e2.resistance, this.resistanceType = e2.resistanceType ?? "quadratic", this.min = e2.min, this.max = e2.max, this.bounceStiffness = e2.bounceStiffness ?? M, this.bounceDamping = e2.bounceDamping ?? v, this.restDelta = e2.restDelta ?? L;
  }
  step(e2, t2, s) {
    const i2 = Math.min(s, 0.033), { acceleration: r4, resistance: p4, resistanceType: c3, min: d5, max: h4 } = this, { position: o2, velocity: l } = e2, f3 = d5 !== void 0 && o2 < d5, w = h4 !== void 0 && o2 > h4;
    let u4;
    if (f3 || w) {
      const D2 = o2 - (f3 ? d5 : h4), O2 = -this.bounceStiffness * D2, E = -this.bounceDamping * l;
      u4 = O2 + E;
    } else {
      const m2 = t2 > o2 ? 1 : -1;
      let D2;
      c3 === "linear" ? D2 = p4 * l : D2 = p4 * l * Math.abs(l), u4 = m2 * r4 - D2;
    }
    const T = l + u4 * i2;
    let S = o2 + T * i2;
    if (!f3 && !w) {
      const m2 = t2 > o2 ? 1 : -1;
      (m2 > 0 && S > t2 || m2 < 0 && S < t2) && (S = t2);
    }
    return {
      position: S,
      velocity: S === t2 ? 0 : T
    };
  }
  isSettled(e2, t2) {
    return Math.abs(t2 - e2.position) < this.restDelta;
  }
};
var y = class _y {
  /**
   * Create an Integrator from physics config
   *
   * @param config Physics configuration (spring or inertia)
   * @returns Appropriate Integrator instance
   * @throws Error if both spring and inertia are provided
   */
  static from(e2) {
    if (e2.spring && e2.inertia)
      throw new Error(
        "Cannot use both 'spring' and 'inertia' together. Choose one physics type."
      );
    if (e2.inertia)
      return new F({
        acceleration: e2.inertia.acceleration,
        resistance: e2.inertia.resistance,
        resistanceType: e2.inertia.resistanceType,
        min: e2.inertia.min,
        max: e2.inertia.max,
        bounceStiffness: e2.inertia.bounceStiffness,
        bounceDamping: e2.inertia.bounceDamping,
        restDelta: e2.inertia.restDelta
      });
    const t2 = e2.spring ?? { stiffness: 300, damping: 30 };
    if (t2.doubleSpring) {
      const s = t2.doubleSpring;
      let i2;
      return typeof s == "number" ? i2 = s : typeof s == "object" && (i2 = {
        stiffness: s.stiffness,
        damping: s.damping
      }), new H({
        stiffness: t2.stiffness,
        damping: t2.damping,
        follower: i2,
        restDelta: t2.restDelta,
        restSpeed: t2.restSpeed
      });
    }
    return new b({
      stiffness: t2.stiffness,
      damping: t2.damping,
      restDelta: t2.restDelta,
      restSpeed: t2.restSpeed
    });
  }
  /**
   * Create an Integrator from SpringConfig (legacy support)
   * @deprecated Use from({ spring: config }) instead
   */
  static fromSpring(e2) {
    return _y.from({ spring: e2 });
  }
};

// node_modules/.pnpm/@ssgoi+core@4.4.0/node_modules/@ssgoi/core/dist/index.js
var it = Object.defineProperty;
var nt = (e2, n4, t2) => n4 in e2 ? it(e2, n4, { enumerable: true, configurable: true, writable: true, value: t2 }) : e2[n4] = t2;
var d = (e2, n4, t2) => nt(e2, typeof n4 != "symbol" ? n4 + "" : n4, t2);
var st = class {
  constructor() {
    d(this, "listeners", /* @__PURE__ */ new Set());
    d(this, "rafId", null);
    d(this, "isRunning", false);
    d(this, "startTime", Date.now());
    d(this, "lastUpdate", this.startTime);
    d(this, "elapsed", 0);
    d(this, "lagThreshold", 500);
    d(this, "adjustedLag", 33);
    d(this, "maxDeltaTime", 33);
    d(this, "gap", 1e3 / 240);
    d(this, "nextTime", this.gap);
    d(this, "tick", () => {
      const n4 = Date.now();
      let i2 = n4 - this.lastUpdate;
      i2 > this.lagThreshold || i2 < 0 ? (this.startTime += i2 - this.adjustedLag, i2 = this.adjustedLag) : i2 > this.maxDeltaTime && (i2 = this.maxDeltaTime), this.lastUpdate = n4;
      const r4 = n4 - this.startTime, o2 = r4 - this.nextTime;
      if (o2 > 0) {
        const s = i2 / 1e3;
        this.elapsed = r4 / 1e3, this.nextTime += o2 + (o2 >= this.gap ? 4 : this.gap - o2), this.listeners.forEach((c3) => {
          c3(s, this.elapsed);
        });
      }
      this.listeners.size > 0 ? this.rafId = requestAnimationFrame(this.tick) : (this.isRunning = false, this.rafId = null);
    });
  }
  subscribe(n4) {
    return this.listeners.add(n4), this.isRunning || (this.isRunning = true, this.rafId = requestAnimationFrame(this.tick)), () => {
      this.unsubscribe(n4);
    };
  }
  unsubscribe(n4) {
    this.listeners.delete(n4), this.listeners.size === 0 && this.rafId !== null && (cancelAnimationFrame(this.rafId), this.isRunning = false, this.rafId = null);
  }
  /**
   * Configure lag smoothing behavior
   * @param threshold - Time threshold to detect lag (default: 500ms)
   * @param adjustedLag - Max time jump when lag detected (default: 33ms)
   */
  lagSmoothing(n4, t2) {
    this.lagThreshold = n4, this.adjustedLag = Math.min(t2, n4);
  }
  /**
   * Set maximum FPS
   * @param fps - Target FPS (default: 240)
   */
  fps(n4) {
    this.gap = 1e3 / n4, this.nextTime = this.elapsed * 1e3 + this.gap;
  }
  getListenerCount() {
    return this.listeners.size;
  }
  getElapsed() {
    return this.elapsed;
  }
};
var N = new st();
function at(e2) {
  const {
    integrator: n4,
    from: t2,
    to: i2,
    velocity: r4 = 0,
    onUpdate: o2,
    onComplete: s,
    onStart: c3
  } = e2;
  let a2 = { position: t2, velocity: r4 }, f3 = true, y4 = 0, C3 = false;
  const A = (E) => {
    if (f3) {
      if (C3 || (C3 = true, c3 == null || c3()), a2 = n4.step(a2, i2, E), n4.isSettled(a2, i2)) {
        if (y4 += Math.min(E, 0.033), y4 >= x) {
          a2 = { position: i2, velocity: 0 }, f3 = false, o2(a2.position), N.unsubscribe(A), s();
          return;
        }
      } else
        y4 = 0;
      o2(a2.position);
    }
  };
  return N.subscribe(A), {
    stop: () => {
      f3 && (f3 = false, N.unsubscribe(A));
    },
    getPosition: () => a2.position,
    getVelocity: () => a2.velocity,
    isRunning: () => f3
  };
}
var O = 1e3 / 60;
function ct(e2, n4, t2, i2 = 0) {
  let o2 = { position: n4, velocity: i2 }, s = 0;
  const c3 = [];
  for (let a2 = 0; a2 < 600; a2++) {
    const f3 = a2 * O;
    if (c3.push({
      time: f3,
      position: o2.position,
      velocity: o2.velocity
    }), o2 = e2.step(o2, t2, O / 1e3), e2.isSettled(o2, t2)) {
      if (s += O / 1e3, s >= x) {
        c3.push({
          time: (a2 + 1) * O,
          position: t2,
          velocity: 0
        });
        break;
      }
    } else
      s = 0;
  }
  return c3;
}
function W(e2, n4) {
  if (e2.length === 0)
    return { position: 0, velocity: 0 };
  const t2 = e2[0], i2 = e2[e2.length - 1];
  if (n4 <= 0)
    return { position: t2.position, velocity: t2.velocity };
  if (n4 >= i2.time)
    return { position: i2.position, velocity: i2.velocity };
  let r4 = 0, o2 = e2.length - 1;
  for (; r4 < o2 - 1; ) {
    const f3 = Math.floor((r4 + o2) / 2);
    e2[f3].time <= n4 ? r4 = f3 : o2 = f3;
  }
  const s = e2[r4], c3 = e2[o2], a2 = (n4 - s.time) / (c3.time - s.time);
  return {
    position: s.position + (c3.position - s.position) * a2,
    velocity: s.velocity + (c3.velocity - s.velocity) * a2
  };
}
function lt(e2, n4) {
  return e2.map((t2) => n4(t2.position));
}
function ut(e2) {
  const {
    integrator: n4,
    element: t2,
    from: i2,
    to: r4,
    velocity: o2 = 0,
    style: s,
    onComplete: c3,
    onStart: a2
  } = e2, f3 = ct(n4, i2, r4, o2);
  if (f3.length === 0)
    return a2 == null || a2(), c3(), {
      stop: () => {
      },
      getPosition: () => r4,
      getVelocity: () => 0,
      isRunning: () => false
    };
  const y4 = lt(f3, s), C3 = f3[f3.length - 1], A = C3.time, E = y4[0];
  if (E)
    for (const l of Object.keys(E))
      t2.style[l] = "";
  const g2 = t2.animate(y4, {
    duration: A,
    fill: "forwards",
    easing: "linear",
    composite: "replace"
  });
  let m2 = true;
  const h4 = performance.now();
  return a2 == null || a2(), g2.onfinish = () => {
    m2 && (m2 = false, c3());
  }, {
    stop: () => {
      m2 && (m2 = false, g2.cancel());
    },
    getPosition: () => {
      if (!m2)
        return C3.position;
      const l = performance.now() - h4;
      return W(f3, l).position;
    },
    getVelocity: () => {
      if (!m2)
        return 0;
      const l = performance.now() - h4;
      return W(f3, l).velocity;
    },
    isRunning: () => m2
  };
}
function ft(e2) {
  const { to: n4, onStart: t2, onComplete: i2 } = e2;
  return t2 == null || t2(), i2(), {
    stop: () => {
    },
    getPosition: () => n4,
    getVelocity: () => 0,
    isRunning: () => false
  };
}
var mt = class {
  /**
   * Get bound runner based on animation mode
   *
   * @param options Animation mode options (tick or css)
   * @returns Bound runner function (empty runner if no animation mode specified)
   * @throws Error if both tick and css are provided
   */
  static from(n4) {
    const { tick: t2, css: i2 } = n4;
    if (t2 && i2)
      throw new Error("Cannot use both 'tick' and 'css' options together");
    return i2 ? (r4) => ut({
      ...r4,
      element: i2.element,
      style: i2.style
    }) : t2 ? (r4) => at({
      ...r4,
      onUpdate: t2
    }) : ft;
  }
};
var J = class {
};
var dt = { stiffness: 300, damping: 30 };
var G = class extends J {
  constructor(t2) {
    super();
    d(this, "options");
    d(this, "runner");
    d(this, "controls", null);
    d(this, "isAnimating", false);
    d(this, "currentValue");
    d(this, "currentVelocity", 0);
    d(this, "updateFn");
    if (this.options = {
      from: t2.from ?? 0,
      to: t2.to ?? 1,
      physics: t2.physics,
      onComplete: t2.onComplete ?? (() => {
      }),
      onStart: t2.onStart
    }, this.currentValue = this.options.from, t2.tick)
      this.updateFn = (i2) => {
        var r4;
        return (r4 = t2.tick) == null ? void 0 : r4.call(t2, i2);
      };
    else if (t2.css) {
      const { element: i2, style: r4 } = t2.css;
      this.updateFn = (o2) => {
        const s = r4(o2);
        for (const [c3, a2] of Object.entries(s))
          i2.style[c3] = typeof a2 == "number" ? String(a2) : a2;
      };
    } else
      this.updateFn = () => {
      };
    this.runner = mt.from({
      tick: t2.tick,
      css: t2.css
    });
  }
  /**
   * Sync element state to current progress value
   * Uses the update callback if provided
   */
  syncState() {
    var t2;
    (t2 = this.updateFn) == null || t2.call(this, this.currentValue);
  }
  /**
   * Create Integrator from physics options
   * Priority: integrator factory > inertia/spring config > default spring
   */
  createIntegrator() {
    const t2 = this.options.physics;
    return t2 != null && t2.integrator ? t2.integrator() : t2 != null && t2.spring || t2 != null && t2.inertia ? y.from({
      spring: t2.spring,
      inertia: t2.inertia
    }) : new b(dt);
  }
  runAnimation(t2) {
    this.isAnimating = true, this.controls = this.runner({
      integrator: this.createIntegrator(),
      from: this.currentValue,
      to: t2,
      velocity: this.currentVelocity,
      onStart: this.options.onStart,
      onComplete: () => {
        this.currentValue = t2, this.currentVelocity = 0, this.isAnimating = false, this.controls = null, this.options.onComplete();
      }
    });
  }
  forward() {
    this.stop(), this.runAnimation(this.options.to);
  }
  backward() {
    this.stop(), this.runAnimation(this.options.from);
  }
  reverse() {
    const t2 = this.options.from;
    this.options.from = this.options.to, this.options.to = t2, this.isAnimating && (this.stop(), this.runAnimation(this.options.to));
  }
  stop() {
    this.controls && (this.currentValue = this.controls.getPosition(), this.currentVelocity = this.controls.getVelocity(), this.controls.stop(), this.controls = null), this.isAnimating = false;
  }
  getVelocity() {
    var t2;
    return ((t2 = this.controls) == null ? void 0 : t2.getVelocity()) ?? this.currentVelocity;
  }
  getCurrentValue() {
    var t2;
    return ((t2 = this.controls) == null ? void 0 : t2.getPosition()) ?? this.currentValue;
  }
  getIsAnimating() {
    return this.isAnimating;
  }
  getCurrentState() {
    return {
      position: this.getCurrentValue(),
      velocity: this.getVelocity(),
      from: this.options.from,
      to: this.options.to
    };
  }
  setValue(t2) {
    this.currentValue = t2;
  }
  setVelocity(t2) {
    this.currentVelocity = t2;
  }
  updateOptions(t2) {
    this.options = { ...this.options, ...t2 };
  }
};
var D = class _D extends J {
  constructor(t2) {
    super();
    d(this, "config");
    d(this, "animators", /* @__PURE__ */ new Map());
    d(this, "itemOrder", []);
    d(this, "completedCount", 0);
    d(this, "completedAnimators", /* @__PURE__ */ new Set());
    d(this, "direction", "forward");
    d(this, "idCounter", 0);
    d(this, "rafId", null);
    d(this, "from");
    d(this, "to");
    d(this, "initialState");
    this.config = t2.config, this.from = t2.from, this.to = t2.to, this.initialState = t2.state ?? {
      position: t2.from,
      velocity: 0
    }, this.initializeAnimators();
  }
  generateId() {
    return `spring_${this.idCounter++}`;
  }
  initializeAnimators() {
    this.config.items.forEach((t2, i2) => {
      const r4 = this.generateId(), o2 = new G({
        from: this.from,
        to: this.to,
        physics: t2.physics,
        tick: t2.tick,
        css: t2.css,
        onComplete: () => this.onAnimatorComplete(r4),
        onStart: t2.onStart
      });
      (i2 === 0 || t2.normalizedOffset === 0) && (o2.setValue(this.initialState.position), o2.setVelocity(this.initialState.velocity), o2.syncState()), this.animators.set(r4, {
        id: r4,
        item: t2,
        animator: o2,
        started: false
      }), this.itemOrder.push(r4);
    });
  }
  /**
   * Calculate progress of an animator (0-1)
   * Progress = |position - from| / |to - from|
   */
  getAnimatorProgress(t2) {
    const i2 = t2.animator.getCurrentState(), r4 = Math.abs(i2.to - i2.from);
    return r4 === 0 ? 1 : Math.abs(i2.position - i2.from) / r4;
  }
  /**
   * Get order of springs based on direction
   * Forward: 0, 1, 2, ...
   * Backward: n-1, n-2, ..., 0
   */
  getOrderedIds() {
    return this.direction === "forward" ? this.itemOrder : [...this.itemOrder].reverse();
  }
  onAnimatorComplete(t2) {
    var r4, o2, s, c3, a2, f3;
    const i2 = this.animators.get(t2);
    !i2 || this.completedAnimators.has(t2) || (this.completedAnimators.add(t2), this.completedCount++, (o2 = (r4 = i2.item).onComplete) == null || o2.call(r4), (c3 = (s = this.config).onProgress) == null || c3.call(s, this.completedCount, this.config.items.length), this.completedCount === this.config.items.length && (this.stopScheduler(), (f3 = (a2 = this.config).onEnd) == null || f3.call(a2)));
  }
  /**
   * Check and start springs based on previous spring's progress
   * Called on each frame while animation is running
   */
  checkAndStartSprings() {
    const t2 = this.getOrderedIds();
    for (let i2 = 0; i2 < t2.length; i2++) {
      const r4 = t2[i2], o2 = this.animators.get(r4);
      if (!o2 || o2.started) continue;
      if (i2 === 0) {
        this.startAnimator(r4);
        continue;
      }
      const s = t2[i2 - 1], c3 = this.animators.get(s);
      if (!c3 || !c3.started) continue;
      const a2 = this.getAnimatorProgress(c3), f3 = o2.item.normalizedOffset;
      a2 >= f3 && this.startAnimator(r4);
    }
  }
  startAnimator(t2) {
    const i2 = this.animators.get(t2);
    !i2 || i2.started || (i2.started = true, this.direction === "forward" ? i2.animator.forward() : i2.animator.backward());
  }
  /**
   * Start the scheduler loop that checks spring progress
   */
  startScheduler() {
    const t2 = () => {
      this.checkAndStartSprings(), this.getIsAnimating() && (this.rafId = requestAnimationFrame(t2));
    };
    t2();
  }
  stopScheduler() {
    this.rafId !== null && (cancelAnimationFrame(this.rafId), this.rafId = null);
  }
  getFirstAnimator() {
    var i2;
    const t2 = this.itemOrder[0];
    return t2 ? ((i2 = this.animators.get(t2)) == null ? void 0 : i2.animator) ?? null : null;
  }
  forward() {
    var t2, i2;
    this.stop(), this.direction = "forward", this.completedCount = 0, this.completedAnimators.clear(), this.resetStartedFlags(), (i2 = (t2 = this.config).onStart) == null || i2.call(t2), this.startScheduler();
  }
  backward() {
    var t2, i2;
    this.stop(), this.direction = "backward", this.completedCount = 0, this.completedAnimators.clear(), this.resetStartedFlags(), (i2 = (t2 = this.config).onStart) == null || i2.call(t2), this.startScheduler();
  }
  resetStartedFlags() {
    this.animators.forEach((t2) => {
      t2.started = false;
    });
  }
  stop() {
    this.stopScheduler(), this.animators.forEach((t2) => {
      t2.animator.stop(), t2.started = false;
    });
  }
  reverse() {
    this.direction = this.direction === "forward" ? "backward" : "forward", this.animators.forEach((t2) => {
      if (!t2.started)
        return;
      const i2 = t2.animator.getCurrentState();
      if (i2.position === i2.to) {
        const o2 = new G({
          from: this.to,
          to: this.from,
          physics: t2.item.physics,
          tick: t2.item.tick,
          css: t2.item.css,
          onComplete: () => this.onAnimatorComplete(t2.id),
          onStart: t2.item.onStart
        });
        o2.setValue(this.to), o2.setVelocity(0), o2.syncState(), o2.forward(), t2.animator = o2;
      } else
        t2.animator.reverse();
    });
  }
  getCurrentState() {
    const t2 = this.getFirstAnimator();
    return t2 ? t2.getCurrentState() : {
      position: 0,
      velocity: 0,
      from: 0,
      to: 1
    };
  }
  /**
   * Get current position value from first animator
   */
  getCurrentValue() {
    const t2 = this.getFirstAnimator();
    return (t2 == null ? void 0 : t2.getCurrentValue()) ?? 0;
  }
  /**
   * Get current velocity from first animator
   */
  getVelocity() {
    const t2 = this.getFirstAnimator();
    return (t2 == null ? void 0 : t2.getVelocity()) ?? 0;
  }
  /**
   * Set position value on first animator
   * TODO: Support per-spring control
   */
  setValue(t2) {
    const i2 = this.getFirstAnimator();
    i2 == null || i2.setValue(t2);
  }
  /**
   * Set velocity on first animator
   * TODO: Support per-spring control
   */
  setVelocity(t2) {
    const i2 = this.getFirstAnimator();
    i2 == null || i2.setVelocity(t2);
  }
  /**
   * Check if any animation is currently running
   */
  getIsAnimating() {
    for (const t2 of this.animators.values())
      if (t2.animator.getIsAnimating())
        return true;
    return false;
  }
  /**
   * Create MultiAnimator from existing state
   * Uses first animator's state for initialization
   */
  static fromState(t2, i2) {
    return new _D({ ...i2, state: t2 });
  }
};
var Q = Symbol.for("TRANSITION_STRATEGY");
var ht = (e2) => ({
  runIn: async (n4) => {
    const { currentAnimation: t2 } = e2;
    if (t2 && t2.direction === "out") {
      const r4 = t2.controller.getCurrentValue(), o2 = t2.controller.getVelocity();
      if (t2.controller.stop(), n4.out)
        return {
          config: await n4.out,
          state: { position: r4, velocity: o2 },
          from: 1,
          to: 0,
          direction: "backward"
          // Will actually go 0→1 (backward means toward 'from')
        };
    }
    return n4.in ? {
      config: await n4.in,
      state: {
        position: 0,
        velocity: 0
      },
      from: 0,
      to: 1,
      direction: "forward"
    } : {
      state: {
        position: 0,
        velocity: 0
      },
      from: 0,
      to: 1,
      direction: "forward"
    };
  },
  runOut: async (n4) => {
    const { currentAnimation: t2 } = e2;
    if (t2 && t2.direction === "in") {
      const r4 = t2.controller.getCurrentValue(), o2 = t2.controller.getVelocity();
      if (t2.controller.stop(), n4.in)
        return {
          config: await n4.in,
          state: { position: r4, velocity: o2 },
          from: 0,
          to: 1,
          direction: "backward"
          // Will actually go 1→0 (backward means toward 'from')
        };
    }
    return n4.out ? {
      config: await n4.out,
      state: {
        position: 1,
        velocity: 0
      },
      from: 1,
      to: 0,
      direction: "forward"
    } : {
      state: {
        position: 1,
        velocity: 0
      },
      from: 1,
      to: 0,
      direction: "forward"
    };
  }
});
var gt = () => ({
  runIn: async (e2) => e2.in ? {
    config: await e2.in,
    state: {
      position: 0,
      velocity: 0
    },
    from: 0,
    to: 1,
    direction: "forward"
  } : {
    state: {
      position: 0,
      velocity: 0
    },
    from: 0,
    to: 1,
    direction: "forward"
  },
  runOut: async (e2) => e2.out ? {
    config: await e2.out,
    state: {
      position: 1,
      velocity: 0
    },
    from: 1,
    to: 0,
    direction: "forward"
  } : {
    state: {
      position: 1,
      velocity: 0
    },
    from: 1,
    to: 0,
    direction: "forward"
  }
});
var Z = "data-ssgoi-scope";
var tt = "data-ssgoi-scope-ready";
function Mt() {
  return (e2) => {
    if (e2)
      return e2.setAttribute(Z, ""), queueMicrotask(() => {
        queueMicrotask(() => {
          e2.setAttribute(tt, "true");
        });
      }), () => {
      };
  };
}
function pt(e2) {
  return e2.hasAttribute(tt);
}
function wt(e2) {
  return e2.closest(`[${Z}]`);
}
var yt = (e2) => {
  let n4 = e2.parentElement;
  for (; n4 && n4 !== document.body; ) {
    const t2 = window.getComputedStyle(n4), i2 = t2.overflow + t2.overflowY + t2.overflowX;
    if (i2.includes("auto") || i2.includes("scroll") || n4.scrollHeight > n4.clientHeight || n4.scrollWidth > n4.clientWidth)
      return n4;
    n4 = n4.parentElement;
  }
  return document.documentElement;
};
var vt = (e2) => {
  let n4 = e2.parentElement;
  for (; n4 && n4 !== document.body; ) {
    const t2 = window.getComputedStyle(n4).position;
    if (t2 === "relative" || t2 === "absolute" || t2 === "fixed" || t2 === "sticky")
      return n4;
    n4 = n4.parentElement;
  }
  return document.body;
};
function $(e2) {
  return new Promise((n4) => {
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        n4();
      });
    });
  });
}
function St(e2, n4) {
  var E;
  let t2 = null, i2 = null, r4 = null, o2 = null;
  const s = {
    get currentAnimation() {
      return t2;
    }
  }, c3 = ((E = n4 == null ? void 0 : n4.strategy) == null ? void 0 : E.call(n4, s)) || ht(s), a2 = async (g2) => {
    var S, b2;
    i2 && (i2.remove(), i2 = null);
    const m2 = e2(), h4 = m2.in && m2.in(g2), l = !(n4 != null && n4.strategy) && m2.out ? m2.out(g2) : void 0;
    if (!h4)
      return;
    const T = {
      in: h(h4),
      out: l ? h(l) : void 0
    }, p4 = await c3.runIn(T);
    if (!p4.config) {
      t2 && (t2.direction = "in");
      return;
    }
    const u4 = p4.config;
    (S = u4.prepare) == null || S.call(u4);
    const v3 = k(
      {
        ...u4,
        onEnd: () => {
          var k3;
          t2 = null, (k3 = u4.onEnd) == null || k3.call(u4);
        }
      },
      g2
    );
    u4.wait && await u4.wait();
    const w = D.fromState(p4.state, {
      config: v3,
      from: p4.from,
      to: p4.to
    });
    t2 = { controller: w, direction: "in" }, (b2 = u4.onReady) == null || b2.call(u4), await $(), p4.direction === "forward" ? w.forward() : w.backward();
  }, f3 = async (g2) => {
    var b2, k3;
    i2 = g2;
    const m2 = e2(), h4 = !(n4 != null && n4.strategy) && m2.in ? m2.in(g2) : void 0, l = m2.out && m2.out(g2);
    if (!l)
      return;
    const T = {
      in: h4 ? h(h4) : void 0,
      out: h(l)
    }, p4 = await c3.runOut(T);
    if (!p4.config) {
      t2 && (t2.direction = "out"), i2 && (i2.remove(), i2 = null);
      return;
    }
    const u4 = p4.config;
    S(), (b2 = u4.prepare) == null || b2.call(u4);
    const v3 = k(
      {
        ...u4,
        onEnd: () => {
          var z, V;
          (z = u4.onEnd) == null || z.call(u4), i2 && (i2.remove(), i2 = null), t2 = null, (V = n4 == null ? void 0 : n4.onCleanupEnd) == null || V.call(n4);
        }
      },
      g2
    );
    u4.wait && await u4.wait();
    const w = D.fromState(p4.state, {
      config: v3,
      from: p4.from,
      to: p4.to
    });
    t2 = { controller: w, direction: "out" }, (k3 = u4.onReady) == null || k3.call(u4), await $(), p4.direction === "forward" ? w.forward() : w.backward();
    function S() {
      !r4 || !i2 || (o2 && r4.contains(o2) ? r4.insertBefore(i2, o2) : r4.appendChild(i2));
    }
  };
  let y4 = null, C3 = false;
  const A = (g2) => () => {
    if (C3) return;
    C3 = true;
    const m2 = g2.cloneNode(true);
    y4 ? queueMicrotask(() => {
      document.contains(y4) && f3(m2);
    }) : f3(m2);
  };
  return (g2) => {
    if (g2)
      return requestAnimationFrame(() => {
        r4 = g2.parentElement, o2 = g2.nextElementSibling;
      }), C3 = false, (n4 == null ? void 0 : n4.scope) === "local" ? queueMicrotask(() => {
        y4 = wt(g2), !(y4 && !pt(y4)) && a2(g2);
      }) : a2(g2), A(g2);
  };
}
var P = /* @__PURE__ */ new Map();
var R = null;
var U = false;
function et(e2) {
  if (e2 instanceof HTMLElement && P.has(e2)) {
    const t2 = P.get(e2);
    P.delete(e2), queueMicrotask(() => {
      t2();
    });
  }
  const n4 = e2.childNodes;
  for (let t2 = 0; t2 < n4.length; t2++) {
    const i2 = n4[t2];
    i2 && et(i2);
  }
}
function Ct() {
  U || typeof document > "u" || (U = true, R = new MutationObserver((e2) => {
    for (const n4 of e2) {
      const t2 = n4.removedNodes;
      for (let i2 = 0; i2 < t2.length; i2++) {
        const r4 = t2[i2];
        r4 && et(r4);
      }
    }
  }), document.body ? R.observe(document.body, {
    childList: true,
    subtree: true
  }) : document.addEventListener(
    "DOMContentLoaded",
    () => {
      R == null || R.observe(document.body, {
        childList: true,
        subtree: true
      });
    },
    { once: true }
  ));
}
function At(e2, n4) {
  Ct(), P.set(e2, n4);
}
var L2 = /* @__PURE__ */ new Map();
var X = /* @__PURE__ */ new Map();
var Y = /* @__PURE__ */ new Map();
function Tt(e2, n4, t2) {
  L2.set(e2, n4);
  let i2 = X.get(e2);
  if (i2)
    return i2;
  const r4 = n4;
  return i2 = St(
    () => {
      const o2 = L2.get(e2);
      return o2 || (console.warn(`Transition "${String(e2)}" not found`), {});
    },
    {
      strategy: t2 == null ? void 0 : t2.strategy,
      scope: t2 == null ? void 0 : t2.scope,
      onCleanupEnd: () => {
        L2.get(e2) === r4 && Et(e2);
      }
    }
  ), X.set(e2, i2), i2;
}
function Et(e2) {
  L2.delete(e2), X.delete(e2), Y.delete(e2);
}
function qt(e2, n4 = "manual") {
  const t2 = Tt(
    e2.key,
    {
      in: e2.in,
      out: e2.out
    },
    {
      strategy: e2[Q],
      scope: e2.scope
    }
  );
  if (n4 === "manual")
    return t2;
  let i2 = Y.get(e2.key);
  return i2 || (i2 = (r4) => {
    if (r4) {
      const o2 = t2(r4);
      o2 && At(r4, o2);
    }
  }, Y.set(e2.key, i2), i2);
}
function bt(e2) {
  const n4 = e2.filter((t2) => t2.symmetric).map((t2) => ({
    from: t2.to,
    to: t2.from,
    transition: t2.transition
  }));
  return [...e2, ...n4];
}
function Rt(e2) {
  let n4 = false, t2 = 0, i2 = 0, r4 = false;
  const o2 = 50, s = (g2) => {
    if (!e2) return;
    const m2 = g2.touches[0];
    m2 && (r4 = m2.clientX < o2, r4 && (t2 = m2.clientX, i2 = m2.clientY));
  }, c3 = (g2) => {
    if (!e2 || !r4) return;
    const m2 = g2.touches[0];
    if (!m2) return;
    const h4 = m2.clientX - t2, l = m2.clientY - i2;
    h4 > window.outerWidth / 2 - 5 && Math.abs(h4) > Math.abs(l) ? n4 = true : n4 = false;
  }, a2 = () => {
    e2 && (r4 = false, t2 = 0, i2 = 0);
  }, f3 = () => n4, y4 = () => {
    typeof window > "u" || e2 && (window.addEventListener("touchstart", s, { passive: true }), window.addEventListener("touchmove", c3, { passive: true }), window.addEventListener("touchend", a2, { passive: true }));
  }, C3 = () => {
    typeof window > "u" || (window.removeEventListener("touchstart", s), window.removeEventListener("touchmove", c3), window.removeEventListener("touchend", a2));
  };
  let A = 0;
  return {
    initialize: y4,
    cleanup: C3,
    isSwipePending: f3,
    resetSwipeDetection: () => {
      requestAnimationFrame(() => {
        A++, A > 1 && (A = 0, n4 = false);
      });
    }
  };
}
function I2(e2, n4) {
  if (n4 === "*")
    return true;
  if (n4.endsWith("/*")) {
    const t2 = n4.slice(0, -1);
    return e2.startsWith(t2) && e2.length > t2.length;
  }
  return e2 === n4;
}
function K(e2, n4, t2) {
  for (const i2 of t2)
    if (I2(e2, i2.from) && I2(n4, i2.to))
      return i2.transition;
  for (const i2 of t2)
    if ((i2.from === "*" || I2(e2, i2.from)) && (i2.to === "*" || I2(n4, i2.to)))
      return i2.transition;
  return null;
}
function Pt(e2 = {}) {
  const { preserveScroll: n4 = false, resetPatterns: t2 = [] } = e2, i2 = (h4) => t2.some((l) => I2(h4, l));
  let r4 = null, o2 = null;
  const s = /* @__PURE__ */ new Map();
  let c3 = null, a2 = false;
  const f3 = () => {
    r4 && c3 && !a2 && s.set(c3, {
      x: r4.scrollLeft,
      y: r4.scrollTop
    });
  }, y4 = (h4) => {
    if (!r4 || !n4) return;
    if (i2(h4)) {
      r4.scrollTo({ top: 0, left: 0 });
      return;
    }
    const l = s.get(h4);
    if (!l) {
      r4.scrollTo({ top: 0, left: 0 });
      return;
    }
    const T = 10;
    let p4 = 0;
    const u4 = () => {
      if (!r4) return;
      r4.scrollTo({
        top: l.y,
        left: l.x
      });
      const v3 = r4.scrollTop, w = r4.scrollLeft;
      !(Math.abs(v3 - l.y) < 1 && Math.abs(w - l.x) < 1) && p4 < T && (p4++, requestAnimationFrame(u4));
    };
    requestAnimationFrame(u4);
  };
  return {
    initializeContext: (h4, l) => {
      a2 = true, o2 = h4, r4 || (r4 = yt(h4), (r4 === document.documentElement ? window : r4).addEventListener("scroll", f3, {
        passive: true
      })), c3 = l, y4(l);
      const T = 10;
      let p4 = 0;
      const u4 = () => {
        p4++, p4 >= T ? a2 = false : requestAnimationFrame(u4);
      };
      requestAnimationFrame(u4);
    },
    calculateScrollOffset: (h4, l) => {
      const T = h4 && s.has(h4) ? s.get(h4) : { x: 0, y: 0 }, p4 = l && i2(l);
      p4 && s.set(l, { x: 0, y: 0 });
      const u4 = p4 ? { x: 0, y: 0 } : l && s.has(l) ? s.get(l) : { x: 0, y: 0 };
      return {
        x: -u4.x + T.x,
        y: -u4.y + T.y
      };
    },
    getScrollContainer: () => r4,
    getPositionedParentElement: () => o2 ? vt(o2) : document.body,
    getScrollPosition: (h4) => h4 && s.has(h4) ? s.get(h4) : { x: 0, y: 0 }
  };
}
function kt() {
  let e2 = null;
  function n4() {
    if (e2 != null && e2.from && (e2 != null && e2.to) && (e2 != null && e2.outResolve) && (e2 != null && e2.inResolve)) {
      const t2 = { from: e2.from, to: e2.to };
      e2.outResolve(t2), e2.inResolve(t2), e2 = null;
    }
  }
  return {
    trigger(t2, i2) {
      e2 || (e2 = {}), i2 === "out" ? e2.from = t2 : e2.to = t2;
    },
    get(t2) {
      return t2 === "in" && (!e2 || !e2.from) ? Promise.resolve(null) : new Promise((i2) => {
        e2 || (e2 = {}), t2 === "out" ? e2.outResolve = i2 : e2.inResolve = i2, n4();
      });
    }
  };
}
function Vt() {
  let e2 = null;
  function n4() {
    var i2, r4;
    e2 && ((i2 = e2.outResolve) == null || i2.call(e2, null), (r4 = e2.inResolve) == null || r4.call(e2, null), e2 = null);
  }
  function t2() {
    if (e2 != null && e2.from && (e2 != null && e2.to) && (e2 != null && e2.outResolve) && (e2 != null && e2.inResolve)) {
      const i2 = { from: e2.from, to: e2.to };
      e2.outResolve(i2), e2.inResolve(i2), e2 = null;
    }
  }
  return {
    trigger(i2, r4) {
      e2 && (r4 === "out" && e2.from && e2.from !== i2 || r4 === "in" && e2.to && e2.to !== i2) && n4(), e2 || (e2 = {}), r4 === "out" ? e2.from = i2 : e2.to = i2;
    },
    get(i2) {
      return new Promise((r4) => {
        e2 || (e2 = {}), i2 === "out" ? e2.outResolve = r4 : e2.inResolve = r4, t2();
      });
    }
  };
}
function _t(e2, n4) {
  const {
    transitions: t2 = [],
    defaultTransition: i2,
    middleware: r4 = (v3, w) => ({ from: v3, to: w }),
    // Identity function as default
    skipOnIosSwipe: o2 = true,
    // Default to true - skip animations on iOS swipe
    experimentalPreserveScroll: s = false,
    // Default to false - manual scroll management
    scrollResetPatterns: c3 = []
    // Default to empty array - no routes reset scroll
  } = e2, { outFirst: a2 = true, createNavigationDetector: f3 } = n4 || {}, y4 = (f3 == null ? void 0 : f3()) ?? (a2 ? kt() : Vt()), C3 = bt(t2), {
    initializeContext: A,
    calculateScrollOffset: E,
    getScrollContainer: g2,
    getPositionedParentElement: m2,
    getScrollPosition: h4
  } = Pt({
    preserveScroll: s,
    resetPatterns: c3
  }), l = Rt(o2);
  l.initialize();
  const T = async (v3, w) => {
    if (l.isSwipePending())
      return l.resetSwipeDetection(), w === "in" ? async (F2) => ({
        onReady: () => {
          F2.style.visibility = "visible";
        }
      }) : () => ({});
    y4.trigger(v3, w);
    const S = await y4.get(w);
    if (!S) return () => ({});
    const { from: b2, to: k3 } = r4(
      S.from,
      S.to
    ), V = K(
      b2,
      k3,
      C3
    ) || i2;
    if (!V) return () => ({});
    const H2 = E(S.from, S.to);
    if (w === "out") {
      const F2 = {
        scrollOffset: H2,
        scroll: h4(S.from),
        get scrollingElement() {
          return g2() || document.documentElement;
        },
        get positionedParent() {
          return m2();
        }
      };
      return (x2) => V.out(x2, F2);
    } else {
      const F2 = {
        scrollOffset: H2,
        get scroll() {
          return h4(S.to);
        },
        get scrollingElement() {
          return g2() || document.documentElement;
        },
        get positionedParent() {
          return m2();
        }
      };
      return async (x2) => {
        const q = await Promise.resolve(V.in(x2, F2)), _2 = q.onReady;
        return q.onReady = () => {
          x2.style.visibility = "visible", _2 == null || _2();
        }, q;
      };
    }
  }, p4 = (v3) => ({
    key: v3,
    in: async (w) => (A(w, v3), (await T(v3, "in"))(w)),
    out: async (w) => (await T(v3, "out"))(w),
    // Add page transition strategy for page-level transitions
    [Q]: gt
  }), u4 = (v3, w) => {
    if (l.isSwipePending())
      return false;
    const { from: S, to: b2 } = r4(v3, w);
    return !!(K(
      S,
      b2,
      C3
    ) || i2);
  };
  return f3 ? {
    getTransition: p4,
    hasMatchingTransition: u4
  } : p4;
}

// node_modules/.pnpm/@ssgoi+react@4.2.1/node_modules/@ssgoi/react/dist/react/src/lib/transition.js
var o = (r4) => {
  if (!r4.key)
    throw new Error(
      "[ssgoi] transition() requires a 'key' property. Either provide it manually or use plugin for auto-injection. import plugin from @ssgoi/react/unplugin/webpack; see: https://ssgoi.dev/en/docs/core-concepts/react-plugin"
    );
  return qt(
    r4,
    "auto"
  );
};

// node_modules/.pnpm/@ssgoi+react@4.2.1/node_modules/@ssgoi/react/dist/core/dist/types.js
function p2(e2) {
  return ("tick" in e2 || "css" in e2) && !("items" in e2);
}
function u2(e2) {
  return "items" in e2;
}
function d2(e2) {
  return "css" in e2 && typeof e2.css == "function";
}
function h2(e2) {
  return "tick" in e2 && typeof e2.tick == "function";
}
async function k2(e2) {
  const n4 = await e2;
  return u2(n4) ? {
    prepare: n4.prepare,
    wait: n4.wait,
    onStart: n4.onStart,
    onEnd: n4.onEnd,
    items: n4.items,
    schedule: n4.schedule
  } : {
    prepare: n4.prepare,
    wait: n4.wait,
    onStart: n4.onStart,
    onEnd: n4.onEnd,
    items: [
      {
        physics: n4.physics,
        tick: n4.tick,
        css: n4.css
      }
    ],
    schedule: "parallel"
  };
}
function y3(e2, n4) {
  const a2 = e2.schedule ?? "parallel", c3 = e2.items.map(
    (s) => {
      let t2;
      switch (a2) {
        case "parallel":
          t2 = 0;
          break;
        case "sequential":
          t2 = 1;
          break;
        case "stagger":
          t2 = Math.max(0, Math.min(1, s.offset ?? 0));
          break;
        default:
          t2 = 0;
      }
      const { css: i2, offset: f3, ...r4 } = s, l = i2 ? typeof i2 == "function" ? n4 ? { element: n4, style: i2 } : void 0 : { element: i2.element, style: i2.style } : void 0;
      return { ...r4, css: l, normalizedOffset: t2 };
    }
  ), { schedule: m2, ...o2 } = e2;
  return {
    ...o2,
    items: c3
  };
}

// node_modules/.pnpm/@ssgoi+react@4.2.1/node_modules/@ssgoi/react/dist/react/src/lib/ssgoi.js
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var import_react2 = __toESM(require_react(), 1);

// node_modules/.pnpm/@ssgoi+react@4.2.1/node_modules/@ssgoi/react/dist/react/src/lib/context.js
var import_react = __toESM(require_react(), 1);
var t = (0, import_react.createContext)(null);
var n2 = t.Provider;
var i = () => {
  const o2 = (0, import_react.useContext)(t);
  if (!o2)
    throw new Error("useSsgoi must be used within SsgoiProvider");
  return o2;
};

// node_modules/.pnpm/@ssgoi+react@4.2.1/node_modules/@ssgoi/react/dist/react/src/lib/ssgoi.js
function d3(t2) {
  const n4 = (0, import_react2.useRef)(null), r4 = (0, import_react2.useRef)(null);
  t2 !== null && n4.current !== t2 && (r4.current = n4.current, n4.current = t2);
  const e2 = (0, import_react2.useRef)({ from: null, to: "" });
  return t2 !== null && (e2.current = { from: r4.current, to: t2 }), (0, import_react2.useCallback)(() => e2.current, []);
}
function p3(t2) {
  return {
    trigger() {
    },
    get(n4) {
      const { from: r4, to: e2 } = t2();
      return !r4 || !e2 || r4 === e2 ? Promise.resolve(null) : Promise.resolve({ from: r4, to: e2 });
    }
  };
}
var C = import_react2.default.memo(
  ({ config: t2, children: n4, usePathname: r4 }) => {
    const e2 = r4?.() ?? null, o2 = d3(e2), l = (0, import_react2.useMemo)(() => {
      const { getTransition: s, hasMatchingTransition: f3 } = _t(t2, {
        createNavigationDetector: r4 ? () => p3(o2) : Vt
      });
      return {
        getTransition: s,
        getInitialStyle: () => {
          if (!r4)
            return {};
          const { from: i2, to: u4 } = o2();
          return !i2 || i2 === u4 ? {} : f3(i2, u4) ? { visibility: "hidden" } : {};
        }
      };
    }, [t2, r4, o2]);
    return (0, import_jsx_runtime.jsx)(n2, { value: l, children: n4 });
  }
);

// node_modules/.pnpm/@ssgoi+react@4.2.1/node_modules/@ssgoi/react/dist/react/src/lib/ssgoi-transition.js
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var import_react3 = __toESM(require_react(), 1);

// node_modules/.pnpm/@ssgoi+react@4.2.1/node_modules/@ssgoi/react/dist/react/src/lib/utils.js
function f2(...o2) {
  return (t2) => {
    o2.forEach((e2) => e2?.(t2));
  };
}
function c2(o2, t2) {
  const e2 = {};
  for (const n4 of Object.keys(t2)) {
    const r4 = o2.style[n4];
    r4 && (e2[n4] = r4);
  }
  return e2;
}

// node_modules/.pnpm/@ssgoi+react@4.2.1/node_modules/@ssgoi/react/dist/react/src/lib/ssgoi-transition.js
var h3 = ({
  children: m2,
  id: o2,
  as: a2,
  className: R2,
  ...p4
}) => {
  const { getTransition: g2, getInitialStyle: S } = i(), y4 = a2 || "div", e2 = (0, import_react3.useRef)(null), i2 = (0, import_react3.useRef)(true), t2 = (0, import_react3.useRef)(null);
  t2.current === null && (t2.current = S());
  let n4;
  i2.current ? n4 = t2.current : e2.current && t2.current && (n4 = c2(
    e2.current,
    t2.current
  ));
  const s = (0, import_react3.useCallback)((l) => {
    e2.current = l, l && (i2.current = false);
  }, []), c3 = g2(o2), f3 = (0, import_react3.useMemo)(
    () => o(c3),
    [c3]
  ), b2 = (0, import_react3.useMemo)(
    () => f2(s, f3),
    [s, f3]
  );
  return (0, import_jsx_runtime2.jsx)(
    y4,
    {
      ref: b2,
      "data-ssgoi-transition": o2,
      className: R2,
      style: n4,
      ...p4,
      children: m2
    }
  );
};

// node_modules/.pnpm/@ssgoi+react@4.2.1/node_modules/@ssgoi/react/dist/react/src/lib/transition-scope.js
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
function n3({ children: o2 }) {
  return (0, import_jsx_runtime3.jsx)("div", { ref: Mt(), style: { display: "contents" }, children: o2 });
}
export {
  C as Ssgoi,
  h3 as SsgoiTransition,
  n3 as TransitionScope,
  d2 as isCssAnimation,
  u2 as isMultiAnimation,
  p2 as isSingleAnimation,
  h2 as isTickAnimation,
  y3 as normalizeSchedule,
  k2 as normalizeToMultiAnimation,
  o as transition
};
//# sourceMappingURL=@ssgoi_react.js.map
